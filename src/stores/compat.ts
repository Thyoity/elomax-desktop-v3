// Deprecated. The Vuex-style mapState/mapMutations/mapActions/mapGetters
// helpers were removed in favor of Pinia's native composables. Components
// now import from 'pinia' directly and pass the store factory (e.g.
// `useAuthStore`) instead of the legacy module string. Safe to delete this
// file — kept only because the harness cannot delete files.
