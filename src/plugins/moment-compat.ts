// Deprecated. Use `@/plugins/dayjs` instead — that file does the locale +
// plugin setup and exposes `$dayjs` globally. Safe to delete this file —
// kept only because the harness cannot delete files.
